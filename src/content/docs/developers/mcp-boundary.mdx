---
title: "Docs MCP Boundary (future)"
description: "Read-only future interface — contract only, no privileged writes."
---
import Provenance from '../../../components/Provenance.astro';

Future read-only tools (contract, not implementation): `search`, `get_page`,
`get_contract`, `get_schema`, `get_api_operation`, `get_claim`, `get_evidence`,
`get_repo_state`, `get_conformance`. No writes. No privileged access.


<Provenance page="developers/mcp-boundary" />
