---
title: "Claim Graph"
description: "C-001..C-008 — claim → status → evidence → study → cut → reproduction."
---
import Provenance from '../../../components/Provenance.astro';
import graph from '../../../data/graph.json';

Registry: ISR `AUDIT-EVID-001` (incl. section 5, STUDY-011 live update) — the
Knowledge Plane renders claims verbatim, never reinterprets them. Every field
below is copied from the registry; the audit status strings are the
registry's own words. Click-through depth (hypothesis/objection/dataset) lives
in ISR's own registries.

<h3>Claim chains ({graph.claim_graph.claims.length})</h3>

{graph.claim_graph.claims.map((c) => (
  <article class="claim">
    <p class="claim-id">
      <code>{c.id}</code> <span class={`lens st-${c.status.split(' ')[0].toLowerCase()}`}>{c.status}</span>
    </p>
    <blockquote>{c.claim}</blockquote>
    {c.auditStatus && <p class="meta"><strong>Audit status</strong> {c.auditStatus}</p>}
    <ul>
      <li><strong>Study</strong> <code>{c.study}</code></li>
      <li><strong>Sample</strong> {c.sample}</li>
      <li><strong>Evidence class</strong> {c.evidenceClass}</li>
      <li><strong>Evidence cut</strong> {c.evidenceCut}</li>
      <li><strong>Reproduction</strong> <code>{c.reproduction}</code></li>
      <li><strong>Source</strong> <code>{c.sourceSha.slice(0, 8)}</code></li>
    </ul>
    <p class="chain">{c.chain.join(' → ')}</p>
  </article>
))}

Status vocabulary (registry-canonical): SUPPORTED states are sample-bounded;
UNTESTED means preregistered without trials; REVERSED means a live
replication contradicted the deterministic direction. Claims are never
silently upgraded.

<Provenance page="evidence/claim-graph" />