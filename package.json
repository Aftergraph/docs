{
  "name": "@aftergraph/docs",
  "version": "0.1.0",
  "description": "Aftergraph Knowledge Plane \u2014 public compiler/rendering/discovery layer over canonical sources. Owns no specs, claims, contracts or product docs.",
  "type": "module",
  "scripts": {
    "build": "node scripts/llms.mjs && node scripts/graph.mjs && node scripts/system-map.mjs && node scripts/validate.mjs && node scripts/freshness.mjs && node scripts/status.mjs && astro build && node scripts/build-manifest.mjs",
    "check": "tsc --noEmit",
    "validate": "node scripts/validate.mjs",
    "freshness": "node scripts/freshness.mjs",
    "prebuild": "node scripts/validate.mjs",
    "predev": "node scripts/status.mjs"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.42.0",
    "astro": "^7.3.1"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
